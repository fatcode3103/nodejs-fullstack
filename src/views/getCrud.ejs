<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Get User</title>
    <style>
        #customers {
            font-family: Arial, Helvetica, sans-serif;
            border-collapse: collapse;
            width: 100%;
        }

        #customers td, #customers th {
            border: 1px solid #ddd;
            padding: 8px;
        }

        #customers tr:nth-child(even){
            background-color: #f2f2f2; 
            transition: all 0.2s linear;
        }

        #customers tr:hover {background-color: #ddd;}

        #customers th {
            padding-top: 12px;
            padding-bottom: 12px;
            text-align: left;
            background-color: #04AA6D;
            color: white;
        }
        button{
            cursor: pointer;
        }

        /* CSS */
        .btn-edit {
        align-items: center;
        appearance: none;
        background-image: radial-gradient(100% 100% at 100% 0, #5adaff 0, #5468ff 100%);
        border: 0;
        border-radius: 6px;
        box-shadow: rgba(45, 35, 66, .4) 0 2px 4px,rgba(45, 35, 66, .3) 0 7px 13px -3px,rgba(58, 65, 111, .5) 0 -3px 0 inset;
        box-sizing: border-box;
        color: #fff;
        cursor: pointer;
        display: inline-flex;
        font-family: "JetBrains Mono",monospace;
        height: 22px;
        width: 40px;
        justify-content: center;
        line-height: 1;
        list-style: none;
        overflow: hidden;
        padding-left: 16px;
        padding-right: 16px;
        position: relative;
        text-align: left;
        text-decoration: none;
        transition: box-shadow .15s,transform .15s;
        user-select: none;
        -webkit-user-select: none;
        touch-action: manipulation;
        white-space: nowrap;
        will-change: box-shadow,transform;
        font-size: 12px;
        }

        .btn-edit:focus {
        box-shadow: #3c4fe0 0 0 0 1.5px inset, rgba(45, 35, 66, .4) 0 2px 4px, rgba(45, 35, 66, .3) 0 7px 13px -3px, #3c4fe0 0 -3px 0 inset;
        }

        .btn-edit:hover {
        box-shadow: rgba(45, 35, 66, .4) 0 4px 8px, rgba(45, 35, 66, .3) 0 7px 13px -3px, #3c4fe0 0 -3px 0 inset;
        transform: translateY(-2px);
        }

        .btn-edit:active {
        box-shadow: #3c4fe0 0 3px 7px inset;
        transform: translateY(2px);
        }

        /* CSS */
        .btn-delete {
        background-image: linear-gradient(45deg, red, rgb(151, 95, 46));
        border: 1px solid #FF4742;
        border-radius: 6px;
        box-sizing: border-box;
        color: #FFFFFF;
        cursor: pointer;
        font-family: nunito,roboto,proxima-nova,"proxima nova",sans-serif;
        font-size: 12px;
        line-height: 1px;
        outline: 0;
        text-align: center;
        touch-action: manipulation;
        vertical-align: middle;
        padding: 4px;
        width: 44px;
        height: 24px;
        }

        .btn-delete:hover,
        .btn-delete:active {
        background-color: initial;
        background-position: 0 0;
        color: #FF4742;
        }

        .btn-delete:active {
        opacity: .5;
        }
    </style>
</head>
<body>
    <div>
        <table id="customers">
            <tr>
                <th>ID</th>
                <th>Email</th>
                <th>Password</th>
                <th>Full Name</th>
                <th>Gender</th>
                <th>Address</th>
                <th>RoleID</th>
                <th>Phone Number</th>
                <th>Actions</th>
            </tr>
            <% for(let i=0; i<data.length; i++) {%>
                <tr>
                    <td><%= data[i].id%></td>
                    <td><%= data[i].email%></td>
                    <td><%= data[i].password%></td>
                    <td><%= data[i].firstName%> <%= data[i].lastName%></td>
                    <td><%= data[i].gender ? 'Male' : 'Female' %></td>
                    <td><%= data[i].address%></td>
                    <td><% if(data[i].roleId === "R1"){ %>
                            <p>Admin</p>
                        <%} else if(data[i].roleId === "R2") {%>
                            <p>Doctor</p>
                        <%} else if(data[i].roleId==="R3" ) {%>
                            <p>Patient</p>
                        <%}%>
                    </td>
                    <td><%= data[i].phoneNumber%></td>
                    <td>
                        <a href="/edit-crud?id=<%= data[i].id%>" class="btn-edit">Edit</a>
                        <a href="/delete-crud?id=<%= data[i].id%>" class="btn-delete">Delete</a>
                    </td>
                </tr>
            <%}%>
        </table>
    </div>
</body>
</html>